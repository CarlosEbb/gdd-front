---
interface Props {
  hasError?: boolean
  isEmpty?: boolean
}

const { hasError = false, isEmpty = false } = Astro.props
---

{hasError ? <slot name="error" /> : isEmpty ? <slot name="empty" /> : <slot />}
