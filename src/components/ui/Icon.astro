---
import { icons, Info, type IconProps } from '@lucide/astro'

interface Props extends IconProps {
  name: keyof typeof icons
  strokeWidth?: number
}

const { name, strokeWidth = 2, ...restProps } = Astro.props
const IconComponent = icons[name] || (Info as any)
---

<IconComponent {...restProps} stroke-width={strokeWidth} />
